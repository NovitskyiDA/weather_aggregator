<% if @weather_information %>
  $('.locations-block').prepend("<%= j render @weather_information.location %>");
<% else %>
  $.notify('You have already added this location', 'success');
<% end %>

$('.abc').spin(false);
